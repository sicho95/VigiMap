import{BaseAdapter}from'./BaseAdapter.js';
export class LtaSingaporeAdapter extends BaseAdapter{constructor(o){super({id:'lta',name:'LTA Singapore',...o});}async fetchCameras(){const d=await this.fetch('https://api.data.gov.sg/v1/transport/traffic-images');return(d?.value?.cameras||[]).map(c=>this.normalize({id:'lta_'+c.camera_id,name:'LTA '+c.camera_id,lat:+c.location?.latitude||0,lng:+c.location?.longitude||0,snapshotUrl:c.image,status:'live',isLive:false,country:'SG'}));}}
