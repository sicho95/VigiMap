import{BaseAdapter}from'./BaseAdapter.js';
export class CamViewerAdapter extends BaseAdapter{constructor(o){super({id:'camviewer',name:'CamViewer',...o});}async fetchCameras(){try{const d=await this.fetch('https://camviewer.live/api/cameras?limit=100&format=json');const l=typeof d==='string'?JSON.parse(d):d;return(l.cameras||l||[]).filter(c=>c.lat&&c.lng).slice(0,100).map(c=>this.normalize({id:'cv_'+c.id,name:c.name||'CamViewer',lat:+c.lat,lng:+c.lng,snapshotUrl:c.snapshot||c.thumb||'',streamUrl:c.stream||c.m3u8||'',isLive:!!(c.stream||c.m3u8),status:(c.stream||c.m3u8)?'live':'delayed',country:c.country||''}));}catch{return[];}}}
