import{BaseAdapter}from'./BaseAdapter.js';
export class OsmOverpassAdapter extends BaseAdapter{constructor(o){super({id:'overpass',name:'OSM Overpass',...o});}async fetchCameras(b){if(!b) return[];const q='[out:json][timeout:25];(node["man_made"="surveillance"]('+b.south+','+b.west+','+b.north+','+b.east+'););out body 200;';try{const d=await this.fetch('https://overpass-api.de/api/interpreter',{method:'POST',body:'data='+encodeURIComponent(q)});const j=typeof d==='string'?JSON.parse(d):d;return(j.elements||[]).filter(e=>e.lat&&e.lon).map(e=>this.normalize({id:'osm_'+e.id,name:e.tags?.name||'OSM Camera',lat:+e.lat,lng:+e.lon,snapshotUrl:'',status:'unknown',isLive:false,country:e.tags?.['addr:country']||''}));}catch{return[];}}}
